---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pocketchat-data-claim
  labels:
    placement: pocketchat-placement-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

